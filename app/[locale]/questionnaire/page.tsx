import { CandidateForm } from "@/components/CandidateForm";

const questionnaire = () => {
  return (
    <main className="min-h-screen bg-gray-50 p-4">
      <CandidateForm candidateData={null} candidateId={null} />
    </main>
  );
};

export default questionnaire;
